// import { Transform } from "../components/Transform";
import { AbstractMnager } from "..//core/implement/AbstractManager";
// import { GameObject } from "./gameobject/implement/data/GameObject";

export class Transformer extends AbstractMnager {

    // update = () => {
    //     const entities: GameObject[] = [...this.game.stage.Children]
    //     while(entities.length) {
    //         const entity = entities.shift()
    //         entities.push(...entity.Children)
    //         const parentTransform = entity.Parent.transform
    //         if(!parentTransform) continue
    //         const transform = entity.transform
    //         transform.position.x = parentTransform.position.x + transform.offsetX
    //         transform.position.y = parentTransform.position.y + transform.offsetY
    //     }
    // }
}