export enum ComponentNameSpace{
    POSITION_2D = 1,

    RENDERER_2D = 2,
}
export enum ManagerNameSpaces {
    Default = 0,
    TimerManager = 1,
    GameObjectManager =2,
    InputManager = 3,
    TaskManager = 4,
}