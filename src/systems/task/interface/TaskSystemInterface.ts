import {AbstractSystemInterface} from "../../../core/interface/AbstractSystemInterface";

export default interface TaskSystemInterface extends AbstractSystemInterface {
    
}

export const EMPTY_TASK = (...p: any[]): any => {  }